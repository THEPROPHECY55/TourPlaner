<Window x:Class="TourPlaner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TourPlaner.ViewModels"
        Title="Tour Planner" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="File">
                    <MenuItem Header="Exit" Click="Exit_Click"/>
                </MenuItem>
                <MenuItem Header="Help">
                    <MenuItem Header="About" Click="About_Click"/>
                </MenuItem>
            </Menu>
            <StackPanel>
                <TextBlock Text="Available Tours:" FontSize="16" Margin="10"/>
                <ListBox ItemsSource="{Binding Tours}" DisplayMemberPath="Name" 
                         SelectedItem="{Binding SelectedTour}" Height="200" Margin="10"/>
                <TextBlock Text="Tour Description:" FontSize="14" Margin="10"/>
                <TextBlock Text="{Binding SelectedTour.Description}" FontSize="12" Margin="10"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>